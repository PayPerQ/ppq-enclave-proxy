name: Enclave cutover (swap + re-pin)

# Swaps the running enclave to a freshly built image and re-pins the frontend.
#
# WHY THIS IS NOT TRIGGERED BY A MERGE
# -----------------------------------
# Rotating PCR0 invalidates EVERY cached frontend bundle's pin at once. Users
# with a tab already open are pinned to the outgoing measurement, which no
# longer exists, so they all fall back to the non-attested path until they
# reload. Measured on 2026-08-24: enclave capture fell 90% -> 17% and took ~45
# minutes to reach 46%. Chat keeps working throughout — this costs
# confidentiality coverage, not availability — but running it unattended on
# every merge would mean routine merges silently degrading privacy for hours,
# potentially overnight with nobody watching.
#
# So the cutover is manual and gated on the `production-enclave` environment.
# Making it safe to automate needs the frontend to accept a PREVIOUS PCR0
# alongside the current one during rollover; until that exists, a human decides
# when to pay the drain.
on:
  workflow_dispatch:
    inputs:
      pcr0:
        description: 'Expected PCR0 of the built image (from the Enclave build run)'
        required: true
        type: string
      # Enclave sizing (#52 scaling). Defaults are today's box. All three must
      # agree with /etc/nitro_enclaves/allocator.yaml on the host; workers > 1
      # only pays off when cpus > 2.
      cpus:
        description: 'vCPUs for the enclave (even; must fit allocator.yaml)'
        required: false
        default: '2'
        type: string
      memory_mib:
        description: 'Memory for the enclave in MiB (must fit allocator.yaml)'
        required: false
        default: '3072'
        type: string
      workers:
        description: 'Node worker processes inside the enclave (1 = no cluster)'
        required: false
        default: '1'
        type: string

# THE CUTOVER IS ATOMIC ON PURPOSE — there is no skip_repin.
#
# There used to be one, described as "swap and verify only, leave the frontend
# pinned to the old PCR0", which reads like the cautious option. It is the
# opposite. Degradation begins at the SWAP: clients pin the outgoing
# measurement, so the moment the enclave changes they all reject it. The re-pin
# is what ENDS the outage. Skipping it does not defer a cost, it extends one
# indefinitely — and the run still went green, so nothing said so.
#
# Used once, on 2026-09-02: production sat degraded for 35 minutes (18:32Z ->
# 19:08Z) and was noticed by accident. A half-finished cutover is not a
# checkpoint; it is an outage with a friendly name.

concurrency:
  group: enclave-cutover
  cancel-in-progress: false

env:
  AWS_REGION: us-east-1
  INSTANCE_ID: i-0609bf23c4b57a48e
  CHECKOUT: /home/ec2-user/ppq-enclave-proxy
  VERCEL_PROJECT: prj_okP7wMOuvNYiJvbyYBH7bdVgceWx

jobs:
  cutover:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    # Configured 2026-09-02: required reviewer + a deployment branch policy
    # restricting this environment to `main`. The branch policy is the load
    # bearing half — workflow_dispatch runs from ANY branch, so without it
    # anyone with push access could run an edited copy of this file and read
    # the environment's secrets. ENCLAVE_SMOKE_CREDIT_ID and VERCEL_TOKEN are
    # environment-scoped for that reason; repo-scoped secrets get no such gate.
    environment: production-enclave
    permissions:
      contents: read
    steps:
      # Needed for scripts/ci-ssm-wait.sh. The cutover otherwise operates
      # entirely on the box; this checkout is the runner's copy, not what gets
      # measured or swapped.
      # Every input below is interpolated into a remote shell command over SSM.
      # Only maintainers can dispatch this workflow, but a typo should fail
      # here, not as a half-formed command on the production host.
      - name: Validate inputs
        env:
          PCR0: ${{ inputs.pcr0 }}
          CPUS: ${{ inputs.cpus }}
          MEM: ${{ inputs.memory_mib }}
          WORKERS: ${{ inputs.workers }}
        run: |
          set -euo pipefail
          [[ "$PCR0" =~ ^[0-9a-f]{96}$ ]] || { echo "::error::pcr0 must be 96 lowercase hex chars"; exit 1; }
          [[ "$CPUS" =~ ^[0-9]{1,3}$ ]] && [ "$CPUS" -ge 2 ] && [ $((CPUS % 2)) -eq 0 ] || { echo "::error::cpus must be an even integer >= 2"; exit 1; }
          [[ "$MEM" =~ ^[0-9]{3,6}$ ]] && [ "$MEM" -ge 512 ] || { echo "::error::memory_mib must be an integer >= 512"; exit 1; }
          [[ "$WORKERS" =~ ^[0-9]{1,2}$ ]] && [ "$WORKERS" -ge 1 ] && [ "$WORKERS" -le 64 ] || { echo "::error::workers must be an integer 1..64"; exit 1; }
          echo "inputs ok: pcr0=${PCR0:0:8} cpus=$CPUS memory_mib=$MEM workers=$WORKERS"

      - name: Check out this commit
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          persist-credentials: false

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.ENCLAVE_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ENCLAVE_AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Check the built image matches the requested PCR0
        run: |
          set -euo pipefail
          ID=$(aws ssm send-command --instance-ids "$INSTANCE_ID" \
            --document-name AWS-RunShellScript \
            --parameters commands="[\"cat $CHECKOUT/build/PCR.json\"]" \
            --query Command.CommandId --output text)
          bash scripts/ci-ssm-wait.sh "$ID" "$INSTANCE_ID"
          aws ssm get-command-invocation --command-id "$ID" --instance-id "$INSTANCE_ID" \
            --query StandardOutputContent --output text > PCR.json
          BUILT=$(python3 -c 'import json;print(json.load(open("PCR.json"))["PCR0"])')
          echo "built=$BUILT"
          echo "asked=${{ inputs.pcr0 }}"
          # Refuse to swap something other than what the operator reviewed.
          test "$BUILT" = "${{ inputs.pcr0 }}"

      # FAIL CLOSED IF CLIENTS DO NOT ALREADY ACCEPT THIS MEASUREMENT.
      #
      # PayPerQ/PPQdotAI#2733 moved /api/enclave-pin off the Vercel env var and
      # onto attestation/published-pcr.json in this repo. The re-pin step below
      # still patches that env var — which is now only the FALLBACK, consulted
      # when this file is unreachable. So a swap to a measurement absent from
      # accepted_pcr0 leaves the route serving the OLD value, every
      # attestation-verifying client mismatches, and re-pinning Vercel does not
      # fix it. That is a worse outage than the one the re-pin was written for,
      # and it is silent.
      #
      # The accept-list makes the correct order publish-then-swap: list the
      # incoming measurement first, let clients pick it up (the route caches for
      # 5 minutes), then swap. Both measurements are accepted across the
      # window, so nobody falls back at all.
      #
      # This asserts that ordering rather than assuming it. It reads the file
      # from the checkout, which the environment's branch policy pins to `main`,
      # so it cannot be satisfied by editing the file on a side branch.
      - name: Check clients already accept this measurement
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json,sys
          want = "${{ inputs.pcr0 }}".lower()
          doc = json.load(open('attestation/published-pcr.json'))
          accepted = [p.lower() for p in doc.get('accepted_pcr0', [])]
          print('requested:', want)
          print('accepted :', accepted)
          if want in accepted:
              print('OK - clients already accept this measurement')
              sys.exit(0)
          print()
          print('REFUSING TO SWAP. attestation/published-pcr.json does not list this')
          print('measurement, so /api/enclave-pin would keep serving the old one and')
          print('every client that verifies attestation would fall back to the')
          print('non-attested path the moment the enclave changed.')
          print()
          print('Merge a PR to main adding it to accepted_pcr0 FIRST, wait ~5 minutes')
          print('for the route cache to turn over, then re-run this workflow. Set')
          print('`current` and prune the outgoing entry in a second PR once the')
          print('swap is verified.')
          sys.exit(1)
          PY

      # The CMK that gates the provider keys allows kms:Decrypt only for a
      # listed PCR0. Grant the incoming measurement BEFORE the swap, for the
      # same reason clients pre-accept before the swap: afterwards is too late,
      # the thing that is running cannot decrypt.
      #
      # BLOCKING, as of the OpenRouter key moving to the ciphertext channel
      # (#11 step 2). It was advisory while nothing depended on the gated path;
      # now a missed grant means the incoming enclave cannot decrypt its
      # OpenRouter key and falls back to the plaintext channel — it would still
      # serve, but the attestation gate would silently be doing nothing, which
      # is the exact failure mode that let the allow-list rot to two dead
      # measurements unnoticed. Fail the cutover instead.
      - name: Grant the incoming measurement KMS access
        run: |
          set -euo pipefail
          python3 scripts/kms-pcr0-allow.py --grant "${{ inputs.pcr0 }}"

      - name: Swap the running enclave
        run: |
          set -euo pipefail
          # run-host.sh occasionally dies on a socat race before the enclave is
          # up (`set -eu`); the documented remedy is simply to retry.
          for attempt in 1 2 3; do
            ID=$(aws ssm send-command --instance-ids "$INSTANCE_ID" \
              --document-name AWS-RunShellScript --timeout-seconds 900 \
              --parameters commands="[\"cd $CHECKOUT && HOME=/root NITRO_CLI_ARTIFACTS=/home/ec2-user/nitro-artifacts ENCLAVE_CPUS=${{ inputs.cpus }} ENCLAVE_MEMORY_MIB=${{ inputs.memory_mib }} SETTLE_HOST=api.ppq.ai REGION=us-east-1 ENCLAVE_CID=16 EIF=$CHECKOUT/build/ppq-enclave-proxy.eif bash scripts/run-host.sh\"]" \
              --query Command.CommandId --output text)
            bash scripts/ci-ssm-wait.sh "$ID" "$INSTANCE_ID"
            S=$(aws ssm get-command-invocation --command-id "$ID" --instance-id "$INSTANCE_ID" --query Status --output text)
            echo "attempt $attempt -> $S"
            [ "$S" = Success ] && break
            sleep 10
          done
          test "$S" = Success

      # ACME_STORE_KEY_ID switches on the sealed certificate store (#83). The
      # CMK id is NOT a secret — it is the same key id checked into
      # scripts/kms-pcr0-allow.py in this public repo — and it is passed
      # literally so the store's on-switch is visible in the workflow rather
      # than hidden in SSM.
      #
      # send-init.sh reads the persisted blob from /var/lib/ppq-enclave on the
      # parent by default, so a restart re-delivers the certificate the previous
      # boot sealed. Without this variable the enclave runs no seal or unseal at
      # all and reports acme_store:"absent".
      #
      # ACME_DOMAIN turns on in-enclave issuance for the SHADOW hostname only.
      # enclave-direct.ppq.ai already reaches the enclave's own TLS: the SNI
      # split deployed 2026-09-03 routes it as raw TCP while enclave.ppq.ai
      # stays terminated by nginx, which is what lets a TLS-ALPN-01 challenge
      # land on 443 without disturbing production.
      #
      # ACME_DIRECTORY now points at Let's Encrypt PRODUCTION, for the SHADOW
      # hostname only. The gate this was waiting on has been met: a restart was
      # observed reusing the stored certificate with no new order (identical
      # serial across a full enclave swap), repeatedly.
      #
      # SCOPE. ACME_DOMAIN now covers BOTH names, as one SAN certificate and
      # therefore ONE order. The ALPN split is live on the box (2026-09-07), so
      # the CA's acme-tls/1 handshake for enclave.ppq.ai reaches the enclave
      # while every ordinary connection for that name still terminates at nginx.
      #
      # OBTAINING THE CERTIFICATE DOES NOT MOVE ANY TRAFFIC. The enclave will
      # hold a certificate for enclave.ppq.ai and serve it to nobody until the
      # nginx map sends that name's ordinary traffic to :8443. That flip is a
      # separate, reversible step -- one map edit and a reload.
      #
      # THE CEILING IS SHARED. Let's Encrypt's duplicate-certificate limit is
      # scoped to the REGISTERED domain, so the shadow name draws on the same
      # ppq.ai quota as production. Five per week, no undo. The sealed store is
      # what makes that safe -- without it each of the ~7 weekly rotations would
      # spend one and exhaust it in a day.
      #
      # ACME_EMAIL stays unset: optional, and expiry mail is not wanted here.
      - name: Send the init blob
        run: |
          set -euo pipefail
          # Every provider secret is delivered on BOTH channels (#11). boot.sh
          # prefers the attestation-gated KMS ciphertext and falls back to the
          # plaintext, so a KMS problem costs OBSERVABILITY, never the service —
          # /health's key_sources then reports `init-plaintext-after-kms-failure`
          # instead of `kms`, which is the whole point of #85.
          #
          # The ciphertext parameter is NOT SecureString: it is ciphertext, and
          # only an enclave whose PCR0 the CMK names can decrypt it. The host
          # role is denied kms:Encrypt, so a compromised box cannot mint a
          # ciphertext the enclave would accept.
          #
          # Secrets are read from SSM ON THE BOX so plaintext never appears in
          # the SSM command text (which is retained and world-readable to
          # anyone with ssm:GetCommandInvocation) or in Actions logs.
          ID=$(aws ssm send-command --instance-ids "$INSTANCE_ID" \
            --document-name AWS-RunShellScript --timeout-seconds 600 \
            --parameters commands="[\"cd $CHECKOUT && export OPENROUTER_KEY_CIPHERTEXT=\$(aws ssm get-parameter --name /ppq-enclave/openrouter-key-ciphertext --region us-east-1 --query Parameter.Value --output text) OPENROUTER_KEY_PLAINTEXT=\$(aws ssm get-parameter --name /ppq-enclave/openrouter-key --with-decryption --region us-east-1 --query Parameter.Value --output text) VERTEX_SA_KEY_CIPHERTEXT=\$(aws ssm get-parameter --name /ppq-enclave/vertex-sa-key-ciphertext --region us-east-1 --query Parameter.Value --output text) VERTEX_SA_KEY_PLAINTEXT=\$(aws ssm get-parameter --name /ppq-enclave/vertex-sa-key --with-decryption --region us-east-1 --query Parameter.Value --output text) ANTHROPIC_KEY_CIPHERTEXT=\$(aws ssm get-parameter --name /ppq-enclave/anthropic-key-ciphertext --region us-east-1 --query Parameter.Value --output text) ANTHROPIC_KEY_PLAINTEXT=\$(aws ssm get-parameter --name /ppq-enclave/anthropic-key --with-decryption --region us-east-1 --query Parameter.Value --output text) FIREWORKS_KEY_CIPHERTEXT=\$(aws ssm get-parameter --name /ppq-enclave/fireworks-key-ciphertext --region us-east-1 --query Parameter.Value --output text) FIREWORKS_KEY_PLAINTEXT=\$(aws ssm get-parameter --name /ppq-enclave/fireworks-key --with-decryption --region us-east-1 --query Parameter.Value --output text) ENCLAVE_SETTLE_SECRET=\$(aws ssm get-parameter --name /ppq-enclave/settle-secret --with-decryption --region us-east-1 --query Parameter.Value --output text) SAFETY_IDENTIFIER_SECRET=\$(aws ssm get-parameter --name /ppq-enclave/safety-identifier --with-decryption --region us-east-1 --query Parameter.Value --output text) ENCLAVE_WORKERS=${{ inputs.workers }} ACME_RENEWAL_MODE=dns01-ci ACME_RENEWAL_AUTHORITY=1 ACME_CI_TOKEN=\$(aws ssm get-parameter --name /ppq-enclave/acme-ci-token --with-decryption --region us-east-1 --query Parameter.Value --output text) ACME_STORE_KEY_ID=354fe7d0-1eb1-45bf-a581-3de8fe12d87a ACME_DOMAIN=enclave-direct.ppq.ai,enclave.ppq.ai ACME_DIRECTORY=https://acme-v02.api.letsencrypt.org/directory SETTLE_HOST=api.ppq.ai REGION=us-east-1 ENCLAVE_CID=16 && bash scripts/send-init.sh\"]" \
            --query Command.CommandId --output text)
          bash scripts/ci-ssm-wait.sh "$ID" "$INSTANCE_ID"
          test "$(aws ssm get-command-invocation --command-id "$ID" --instance-id "$INSTANCE_ID" --query Status --output text)" = Success

      - name: Deliver Bedrock signing credentials + install the refresh timer
        run: |
          set -euo pipefail
          # The swap boots a fresh enclave with no Bedrock creds (they are
          # short-lived STS creds held only in Node memory, never in the init
          # env). The host assumes the invoke-only role and pushes them over
          # vsock:7001 — once now, then every 30 min via the systemd timer
          # (installed idempotently from the checkout's unit files). No
          # secrets transit SSM or Actions: the assume-role happens ON the
          # box. Delivery uses the plaintext channel mode — the same
          # (not-attestation-gated) posture every bearer key currently ships
          # with; the KMS-enveloped mode stays available once the CMK policy
          # tracks PCR0 rotations.
          ID=$(aws ssm send-command --instance-ids "$INSTANCE_ID" \
            --document-name AWS-RunShellScript --timeout-seconds 300 \
            --parameters commands="[\"install -m 644 $CHECKOUT/scripts/systemd/ppq-bedrock-creds.service $CHECKOUT/scripts/systemd/ppq-bedrock-creds.timer /etc/systemd/system/ && systemctl daemon-reload && systemctl enable --now ppq-bedrock-creds.timer && systemctl start ppq-bedrock-creds.service\"]" \
            --query Command.CommandId --output text)
          bash scripts/ci-ssm-wait.sh "$ID" "$INSTANCE_ID"
          test "$(aws ssm get-command-invocation --command-id "$ID" --instance-id "$INSTANCE_ID" --query Status --output text)" = Success
          # The enclave's health surface confirms the blob was accepted (it
          # rejects malformed/expiration-less creds silently otherwise).
          # Bounded + retried: this runs AFTER the swap, where a black-holed
          # network path hanging until the job timeout would delay the
          # half-finished-cutover alarm by half an hour.
          curl -sf --connect-timeout 5 --max-time 30 --retry 3 --retry-delay 5 \
            https://enclave.ppq.ai/health | python3 -c 'import json,sys; d=json.load(sys.stdin); print(d); assert d.get("bedrockCredsLoaded") is True, "enclave did not accept the Bedrock creds"'

      - name: Verify the running enclave measures as expected
        run: |
          set -euo pipefail
          ID=$(aws ssm send-command --instance-ids "$INSTANCE_ID" \
            --document-name AWS-RunShellScript \
            --parameters commands='["nitro-cli describe-enclaves"]' \
            --query Command.CommandId --output text)
          bash scripts/ci-ssm-wait.sh "$ID" "$INSTANCE_ID"
          aws ssm get-command-invocation --command-id "$ID" --instance-id "$INSTANCE_ID" \
            --query StandardOutputContent --output text > enclaves.json
          python3 - <<'PY'
          import json,sys
          e=json.load(open('enclaves.json'))[0]
          print('state', e['State'], 'flags', e['Flags'], 'pcr0', e['Measurements']['PCR0'])
          # Flags must be NONE: --debug-mode zeroes PCR0, and every pinned client
          # would reject an all-zero measurement.
          ok = e['State']=='RUNNING' and e['Flags']=='NONE'
          sys.exit(0 if ok else 1)
          PY
          grep -q "${{ inputs.pcr0 }}" enclaves.json

      # Now that the new measurement is confirmed running, drop everything from
      # the CMK allow-list except it and the measurement it replaced.
      #
      # The outgoing one is KEPT on purpose, and is read from the POLICY rather
      # than from `published-pcr.json`. Deriving it from the published record
      # broke on a same-measurement restart (redelivering an init blob without
      # changing the image): "previous" then equalled the incoming measurement,
      # the pair deduped to one entry, and the real rollback target silently
      # lost its ability to decrypt. It stays because an emergency rollback
      # has to be able to decrypt too, and there is no rollback EIF — a rebuild
      # is ~25 minutes. That makes this list one entry wider than the client
      # accept-list, which is a deliberate difference: an extra client entry
      # means a retired image is still trusted, an extra entry here means a
      # rollback target still works.
      # Still advisory, unlike the grant above, and the asymmetry is deliberate:
      # a failed GRANT means the incoming enclave cannot decrypt, while a failed
      # PRUNE only leaves a retired measurement able to — worth fixing, not
      # worth failing a completed cutover over. The drift check reports it
      # (`CMK allows N, expected at most 2`), which is how the stale-read bug
      # in #88 was caught.
      - name: Prune the KMS allow-list to {running, previous}
        continue-on-error: true
        run: |
          set -uo pipefail
          python3 scripts/kms-pcr0-allow.py --prune-keeping-previous "${{ inputs.pcr0 }}" || {
            echo "::warning::KMS allow-list prune failed — see #11. Stale entries mean"
            echo "::warning::retired measurements can still decrypt; the drift check reports it."
          }

      - name: Smoke-test a real completion through the enclave
        env:
          CREDIT_ID: ${{ secrets.ENCLAVE_SMOKE_CREDIT_ID }}
        run: |
          set -euo pipefail
          # A plaintext body is accepted (EHBP is optional per-request), so CI can
          # exercise routing + authorization + settle without the browser crypto.
          # openrouter/auto specifically, because it is the request whose upstream
          # choice proves hp's allow-list directive is being applied.
          code=$(curl -s -o resp.txt -w '%{http_code}' -X POST \
            https://enclave.ppq.ai/v1/chat/completions \
            -H 'content-type: application/json' \
            -H "x-credit-id: $CREDIT_ID" -H 'x-query-source: ui' \
            -d '{"model":"openrouter/auto","max_tokens":8,"messages":[{"role":"user","content":"say ok"}]}')
          echo "HTTP $code"; head -c 400 resp.txt; echo
          test "$code" = 200
          python3 - <<'PY'
          import json,os
          d=json.load(open('resp.txt'))
          m=d.get('model','')
          print('router picked:', m)
          open(os.environ['GITHUB_ENV'],'a').write(f'SMOKE_MODEL={m}\n')
          PY

      # THIS IS NO LONGER THE AUTHORITY, ONLY THE FALLBACK.
      #
      # PayPerQ/PPQdotAI#2733 moved /api/enclave-pin onto
      # attestation/published-pcr.json in this repo; NEXT_PUBLIC_ENCLAVE_PCR0
      # is consulted only when that file cannot be fetched. Keeping it current
      # still matters — it is what serves users if GitHub is unreachable — but
      # it is no longer what makes a cutover visible to clients. The accept-list
      # check above is what does that, and it runs BEFORE the swap.
      - name: Re-pin the frontend fallback and redeploy
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json,os,urllib.request
          T=os.environ['VERCEL_TOKEN']; P=os.environ['VERCEL_PROJECT']
          NEW="${{ inputs.pcr0 }}"
          def call(m,u,b=None):
              r=urllib.request.Request(u,method=m,headers={'Authorization':'Bearer '+T,'Content-Type':'application/json'},
                                       data=json.dumps(b).encode() if b else None)
              # Vercel deployment JSON carries raw control chars in commit
              # messages; strict parsing rejects it.
              return json.loads(urllib.request.urlopen(r,timeout=60).read().decode(), strict=False)

          # EVERY scope, not just production. This used to filter on
          # `'production' in target`, so the preview/staging copy was never
          # touched by any cutover — found on 2026-09-02 sitting on fc23c3b9,
          # the v0.5.1 measurement, three releases behind.
          envs=[e for e in call('GET',f'https://api.vercel.com/v10/projects/{P}/env?decrypt=true')['envs']
                if e['key']=='NEXT_PUBLIC_ENCLAVE_PCR0']
          prod=[e for e in envs if 'production' in (e.get('target') or [])]
          assert len(prod)==1, f'expected exactly one production pin, got {prod}'
          for e in envs:
              call('PATCH', f"https://api.vercel.com/v9/projects/{P}/env/{e['id']}", {'value': NEW})
              print('patched', e['id'], e.get('target'), e.get('gitBranch') or '')

          # NEXT_PUBLIC_* is inlined at BUILD time, so the env change alone is
          # inert — production must be rebuilt for clients to see the new pin.
          latest=call('GET',f'https://api.vercel.com/v6/deployments?projectId={P}&target=production&limit=1&state=READY')['deployments'][0]
          out=call('POST','https://api.vercel.com/v13/deployments?forceNew=1',
                   {'name':latest.get('name','pp-qdot-ai'),'deploymentId':latest['uid'],'target':'production'})
          print('production redeploy:', out.get('id'), out.get('readyState'))

          # Non-production scopes are patched but NOT redeployed here. Triggering
          # a deploy in an environment this workflow does not own is a surprise;
          # staging ships several times a day and picks the value up on its next
          # build. Stated so nobody reads "patched" as "live".
          others=[e for e in envs if e not in prod]
          if others:
              print(f'{len(others)} non-production scope(s) patched; they take effect on their next deploy')
          PY

      # A cutover that stops after the swap leaves production degraded, and
      # there is NO rollback: the box keeps one EIF and each build overwrites
      # it, so reverting means a ~25 minute rebuild. Nothing here can undo it,
      # so the least this can do is refuse to look routine.
      - name: Report a half-finished cutover
        if: failure()
        run: |
          {
            echo "## :rotating_light: Cutover did not complete"
            echo
            echo "The enclave may already be swapped to \`${{ inputs.pcr0 }}\` while"
            echo "clients still pin the previous measurement. If so, every client that"
            echo "verifies attestation is falling back to the **non-attested path**"
            echo "right now. Chat keeps working; confidentiality coverage does not."
            echo
            echo "This does not fix itself and a reload does not help — the deployed"
            echo "bundle still carries the old pin."
            echo
            echo "1. \`curl -s https://ppq.ai/api/enclave-pin\` and compare \`pcr0\`"
            echo "   and \`accepted\` with \`nitro-cli describe-enclaves\` on the box."
            echo "2. If the running measurement is not in \`accepted\`, the fix is a PR"
            echo "   adding it to \`accepted_pcr0\` in \`attestation/published-pcr.json\`."
            echo "   That file is the authority — **not** the Vercel env var, which"
            echo "   PayPerQ/PPQdotAI#2733 demoted to a fallback."
            echo "3. Check \`origin\` in that response. \`published\` means the repo file"
            echo "   is being read; \`env\` means GitHub was unreachable and the stale"
            echo "   build-time constant is being served instead."
            echo "4. There is no rollback EIF. Reverting the enclave is a rebuild."
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Summary
        if: success()
        run: |
          {
            echo "## Enclave cutover"
            echo
            echo "| | |"
            echo "|---|---|"
            echo "| PCR0 | \`${{ inputs.pcr0 }}\` |"
            echo "| Smoke model | \`${SMOKE_MODEL:-n/a}\` |"
            echo "| Production re-pinned | yes |"
            echo
            echo "Enclave capture dips while already-open tabs still hold the previous"
            echo "pin, and recovers as they reload. Non-production Vercel scopes were"
            echo "patched but take effect on their next deploy."
          } >> "$GITHUB_STEP_SUMMARY"
